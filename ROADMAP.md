# 🐑🧜‍♀️🐰🐯 한국특허정보원 카드배틀 — 개발 로드맵

> ⚠️ **초기 설계 문서입니다(현재 코드와 다릅니다).** 이후 대개편(6×6 보드 → 장소 클릭 방식,
> 자동발동 효과 → 턴 종료 행동 선택제, 도토리 폭탄 → 도토리 축제 등)을 거치며 실제 구현과
> 달라진 부분이 많습니다. 만들어온 흐름을 되짚는 용도로만 보고,
> **지금의 규칙은 [README](README.md)와 `shared/constants.ts`를,
> 처음 배우는 안내는 [게임 즐기는 법](게임%20즐기는%20법.md)을 보세요.**

> 이 문서는 확정된 게임 규칙을 기반으로, 웹(JavaScript) 구현을 위한 작업(Task)과 세부 단계(Step)를 정의한다.
> 본 버전은 **한국특허정보원** 정체성을 반영한 리스킨판으로, 네 마리 아기 동물이 4대 지식재산권(실용신안·상표·디자인·특허)의 성격을 그대로 체화하도록 이름과 일부 효과를 조정했다.

---

# PART 1. 확정 게임 규칙

## 1-1. 세계관

지식재산의 요람, **한국특허정보원** 어딘가에는 네 가지 권리의 정령들이 자라나고 있다 — **실용신양(實用新羊)·상표토끼·디자인어·특허랑이**. 두 부족(팀)이 이 아기 동물들을 하나씩 깨워(카드를 오픈해) 짝을 지어 데려가는 게임. 아기 동물들은 각자 자신이 대변하는 지식재산권의 특성을 그대로 체화하며 판 전체에 영향을 미친다.

- 🐑 **실용신양**(실용신안 + 양): 정교함보다 물량, 짧고 굵게 반복 출원되는 실용신안의 성격
- 🐰 **상표토끼**(상표 + 토끼): 한번 브랜드 파워가 붙으면 기하급수적으로 불어나는 상표 가치
- 🧜‍♀️ **디자인어**(디자인 + 인어): 트렌드를 좇으며 서로의 디자인을 참고(벤치마킹)해 격차를 좁혀가는 디자인 업계의 생리
- 🐯 **특허랑이**(특허 + 호랑이): 등록까지 오래 걸리지만(20년 존속), 한번 등록되면 압도적인 독점배타권을 행사하는 특허의 위엄

## 1-2. 구성

- 시작 보드: **10×10 = 100장** (동물별 각 25장)
- 각 카드: 동물 그림 + **1~6 무작위 숫자**(성장치)
- 시작 시 전 카드 뒷면 상태

## 1-3. 진행

- **N:N 팀전** (1:1, 2:2, 3:3 …)
- 팀이 번갈아 1장씩 오픈, **A팀 1회 + B팀 1회 = 1턴**
- **오픈 제한시간: 30초** — 초과 시 미오픈 카드 중 무작위 1장이 자동 오픈되어 강제 진행
- 오픈 즉시 판정: 판 위 미획득 동일 동물이 **짝수(2n)장**이면 오픈한 팀이 즉시 전부 획득 / **홀수**면 판에 잔류
- 획득 점수 = 해당 동물 카드들의 숫자 합산

## 1-4. 특수 능력

> ⚠️ **임계값 단위가 동물별로 다름에 유의**: 실용신양·상표토끼는 기존과 동일하게 **10n**, 디자인어·특허랑이는 각 지재권의 법정 존속기간(디자인 20년·특허 20년)에 맞춰 **20n**으로 변경됨.

| 동물 | 컨셉 | 발동 기준 | 효과 |
|---|---|---|---|
| 🐑 실용신양 | "질보다 양(量)" | 자기 팀 점수 **10n** 신규 도달 | 내 차례 플립마다 무작위 카드 **n장 추가 오픈** (연쇄 가능, 상한 없음) |
| 🐰 상표토끼 | "피보나치의 축복" | 자기 팀 점수 **10n** 신규 도달 | 임계값 돌파 턴 종료 시 **1회성으로 `n × 현재턴수`점** 획득 |
| 🧜‍♀️ 디자인어 | "벤치마킹" — 후발주자는 선도 디자인을 참고해 격차를 좁힌다 | 자기 팀 점수 **20n** 신규 도달 | **상대보다 뒤처져 있으면**: 격차의 **50%**를 상대로부터 흡수 / **앞서 있으면**: `round(n × 턴수 × 0.3)`점의 소량 보너스만 획득 |
| 🐯 특허랑이 | "독점의 위엄" — 등록은 늦지만, 한번 등록되면 압도적 | 자기 팀 점수 **20n** 신규 도달 | 상대 팀의 실용신양·상표토끼 점수를 각각 **`round(n × 현재턴수 × 1.5)`점 감소** (최소 0) |

### 1-4-1. 밸런스 시뮬레이션 검증 결과

리스킨 과정에서 임계값을 10n→20n으로 바꾼 두 동물(디자인어, 특허랑이)은 발동 빈도가 절반으로 줄기 때문에, 계수를 그대로 두면 밸런스가 무너진다. 이를 300~1000게임 규모의 봇 대전 시뮬레이션으로 재검증했다.

**특허랑이 계수 조정 (10n/0.5 → 20n/1.5)**
- 기존(10n, 계수 0.5) 기준값: 특허랑이 억제력/토끼 폭증력 비율 ≈ 0.78, 상표토끼 최종점수 ≈ 89.0
- 신규(20n)에서 계수 0.5 그대로 사용 시: 비율 0.29, 상표토끼 최종점수 149.8로 **완전 폭주**
- 신규(20n) 계수 **1.5**로 조정: 비율 0.81, 상표토끼 최종점수 89.1 — **기존 밸런스와 거의 동일하게 재현됨**

**디자인어 효과 변경 (10n 인어 떨림 → 20n 벤치마킹 캐치업 50%)**
- 효과 없음(참고 기준선): 15턴 시점 리더가 끝까지 이기는 "고착률" 72.3%
- 원안(단순히 점수 차이만큼 획득, 방향 무관): 고착률은 59.9%로 낮아지지만 최종 격차 평균이 오히려 50.8로 폭증 — **앞선 팀이 발동하면 격차가 더 벌어지는 부작용 확인, 채택 보류**
- 채택안(조건부 캐치업 50%): 고착률 72.3% → **59.1%**로 감소, 최종 격차 평균 25.7 (기준선 19.7과 비슷한 수준 유지), **뒤처진 팀의 역전 성공률 40.9%**, 게임당 평균 발동 3.3회

## 1-5. 보드 확장

- **20턴 종료 시점**에 초원이 사방으로 넓어져 **14×14(196장)**로 1회 확장
- 신규 96장도 동물 4종 균등(각 24장) + 숫자 1~6 무작위

## 1-6. 종료·승리

- **40턴 도달 시 강제 종료** (또는 그 전에 전 카드 오픈 시 종료)
- 팀별 4개 동물 점수 총합이 높은 팀 승리
- 미획득 잔류 카드·미오픈 카드는 무효

## 1-7. UI 확정 사항

- 아기자기한 동물 얼굴 + 부족/정글 색감(연두·녹색 바탕, 갈색 나뭇가지, 잎사귀 장식)
- **현재 차례 참가자의 닉네임 칩이 하얗게 번쩍이는(글로우 펄스) 효과**
- 동물 점수 아이콘은 **크고 귀엽게** (공간 여유 활용)
- 특허랑이 아래 **"⚔ 현재 공격력"** 표시 — 동물 점수 영역과 같은 가로 너비, 텍스트 중앙 정렬
- 턴 표시 아래 프로그레스바: **맨 왼쪽 모래시계(⏳) 아이콘 + 30초 카운트다운** 시각화
- 반응형 레이아웃 (창 크기에 따라 카드판 크기 변동)
- 카드판 내부: **마우스 휠 확대/축소 + 드래그 패닝** (모바일 터치 포함)
- 모서리 잎사귀 장식: **호버 시 살랑살랑 흔들림**
- 디자인어(캐치업) 관련 별도 게이지/연출은 **추후 확정** (본 문서 시점 기준 미확정 — 다른 UI와 마찬가지로 명시적 확정 후 프로토타입 반영)

## 1-8. 카드 오픈 연출 (숫자별 도파민 단계)

카드가 뒤집힐 때, 그 카드의 **숫자(1~6)**에 따라 리액션 강도가 달라진다. 숫자가 클수록 "더 좋은 카드"라는 인상을 주기 위한 단계별 연출.

| 숫자 | 연출 |
|---|---|
| 1 | 회색 먼지 효과 (픽 하고 꺼지는 듯한 낮은 텐션) |
| 2~3 | 동물 그림이 윙크 |
| 4~5 | 동물 그림이 윙크 + 👍(thumbs up) 마크 추가 |
| 6 | 동물 그림이 윙크 + 👍 + **짧게 금빛으로 빛나며 강조** (하이라이트 최고 등급) |

> 숫자와 무관하게, 카드 자체의 플립 애니메이션(3D rotateY)은 공통으로 먼저 실행되고, 그 위에 위 표의 리액션이 얹히는 구조.

## 1-9. 실용신양 연쇄 오픈 시 화면 흔들림 (단계별 강도)

실용신양 효과로 카드를 여러 장 동시에 추가 오픈하게 될 때, **그 순간의 레벨(n)** 이 높을수록 화면 흔들림이 점점 강해진다.

| 레벨(n) | 연출 |
|---|---|
| 1~4 | 미미한 화면 흔들림 (거의 티 안 남) |
| 5~7 | 화면 흔들림 강도 증가 + **배경에서 나뭇잎 몇 개가 떨어짐** |
| 8 이상 | 위 효과 + **아직 안 뒤집힌 다른 모든 카드가 한 번 들썩임** (전체 보드 반응) |

> 레벨이 오를수록 강도가 "선형적으로" 세지는 게 아니라 위 3개 구간(1~4 / 5~7 / 8+)으로 나뉜 계단식 연출. 낮은 레벨에서 과하게 흔들면 매 턴 피로해지므로, 임팩트는 후반 대폭발 구간에 몰아준다.

---

# PART 2. 구현 분석

## 2-1. 아키텍처 개요

```
[클라이언트 (브라우저)]
  ├─ 렌더링 계층: 보드/카드/점수판/애니메이션 (HTML+CSS+JS 또는 Canvas)
  ├─ 뷰 상태: 줌/팬
  └─ 입력: 카드 클릭, 타이머 UI

[서버 (Node.js + WebSocket)]
  ├─ 게임 상태의 유일한 진실(Source of Truth)
  ├─ 카드 배치·숫자는 서버만 알고 있음 (치팅 방지)
  ├─ 턴/타이머 관리 (30초 강제진행도 서버가 판정)
  └─ 모든 특수효과(실용신양/상표토끼/디자인어/특허랑이)는 서버에서 즉시 점수에 반영, 클라이언트는 결과값만 수신
```

**핵심 설계 원칙**
1. **상태와 뷰의 분리**: 서버는 전체 상태를 알지만, 클라이언트에는 "그 팀이 볼 수 있는 것"만 내려준다. (참고: 디자인어 효과가 기존 "인어 떨림" 방식의 은닉정보 노출에서 순수 점수 보너스 방식으로 변경되면서, 팀별로 다른 정보를 내려줘야 하는 뷰 분기 로직이 사라짐 — 구현이 단순해짐)
2. **좌표 기반 보드**: 고정 2D 배열 대신 `"row,col"` 키의 Map으로 관리 → 14×14 확장 시 기존 좌표 재배치 없이 외곽 링만 추가 가능 (좌표는 음수 허용: 시작 보드가 0~9라면 확장 링은 -2~11).
3. **임계값 판정은 lastLevel 방식**: `floor(score/threshold)`을 매번 비교하지 말고 `lastLevel`을 저장해 "새로 넘은 구간"만 발동 (시뮬레이션 검증 완료된 방식). **동물별 threshold 값이 다름**(실용신양·상표토끼=10, 디자인어·특허랑이=20)에 주의.
4. **모든 랜덤은 서버에서**: 강제진행 카드 선택, 실용신양 효과 추가 오픈 대상, 확장 링 카드 배치 모두 서버 난수.

## 2-2. 게임 상태 모델 (초안)

```js
// 내부 코드상 키는 기존 동물명을 유지하되(sheep/mermaid/rabbit/tiger),
// 화면 표시명은 실용신양/디자인어/상표토끼/특허랑이로 매핑한다.
// key mapping: sheep=실용신양, rabbit=상표토끼, mermaid=디자인어, tiger=특허랑이

const THRESHOLDS = { sheep: 10, rabbit: 10, mermaid: 20, tiger: 20 };
const TIGER_COEF = 1.5;     // 특허랑이 상대 감소 계수
const WOLF_CATCHUP_PCT = 0.5;   // 디자인어, 뒤처졌을 때 격차 흡수 비율
const WOLF_LEAD_BONUS_COEF = 0.3; // 디자인어, 앞섰을 때 소량 보너스 계수

GameState = {
  phase: 'lobby' | 'playing' | 'ended',
  turn: 1..40,
  activeTeam: 'A' | 'B',
  activePlayerIndex: number,       // 팀 내 로테이션
  turnDeadline: timestamp,          // 30초 타이머 기준
  board: Map<"r,c", Card>,
  expanded: boolean,
  teams: {
    A: { members: [...], scores: {sheep, mermaid, rabbit, tiger},
         lastLevel: {sheep, mermaid, rabbit, tiger} },
    B: { ... }
  }
}
Card = { animal, num, open, collectedBy: null|'A'|'B' }
```

---

# PART 3. Task 분해 & 세부 Step

## 📦 Task 0. 프로젝트 셋업 ✅ 완료
**목표**: 개발/배포 가능한 뼈대 마련
1. ✅ 기술 스택 확정 — Next.js 16 (Turbopack) + TypeScript + Tailwind CSS v4 (client), Node.js + ws (server)
2. ✅ 저장소 초기화, 디렉토리 구조 설계 (`/client`, `/server`, `/shared`)
3. ✅ 게임 상수 파일 작성: 보드 크기, 턴 상한(40), 확장 턴(20), **동물별 임계 단위(실용신양·상표토끼=10, 디자인어·특허랑이=20)**, **특허랑이 계수(1.5)**, **디자인어 캐치업 비율(0.5)/리드보너스 계수(0.3)**, 오픈 제한시간(30초)
4. ✅ 로컬 개발 서버 + 핫리로드 세팅 (ts-node + Next.js dev)

## 🧠 Task 1. 코어 게임 엔진 (서버, UI 없이 동작) ✅ 완료
**목표**: 규칙 전체를 순수 로직으로 구현 — 렌더링과 완전 분리, 테스트 가능하게

1. ✅ **Step 1-1. 보드 생성기** (`server/engine/board.ts`)
   - 동물 4종 × 25장 + 숫자 1~6 부여 → 셔플 → `Map<"r,c", Card>` 배치
   - 확장용 링 생성 함수: 기존 side → 새 side 차이만큼 카드 생성(4종 균등), 외곽 좌표에 배치
2. ✅ **Step 1-2. 오픈 & 즉시 판정** (`server/engine/openCard.ts`)
   - `openCard(team, r, c)`: 오픈 처리 → 보드 위 미획득 동일 동물 수 집계 → 짝수면 전부 `collectedBy=team` + 숫자 합산 가점
3. ✅ **Step 1-3. 실용신양 효과** (`server/engine/effects/sheep.ts`)
   - 오픈 직후 `n = floor(sheepScore/10)` 재계산, n장 무작위 추가 오픈
   - 추가 오픈 중 점수가 또 오르면 잔여 오픈 수에 증가분 합산 (연쇄), safety cap = 350
4. ✅ **Step 1-4. 특허랑이 효과** (`server/engine/effects/tiger.ts`)
   - `lastLevel.tiger`와 비교해 **20점 단위**로 신규 돌파 시 `dmg = round(gained × turn × 1.5)` → 상대 실용신양·상표토끼 각각 감소(min 0)
5. ✅ **Step 1-5. 상표토끼 효과** (`server/engine/effects/rabbit.ts`)
   - 턴 종료 시 `lastLevel.rabbit` 비교 → 신규 돌파면 `gained × turn` 가점 → **보너스 반영 후 lastLevel 갱신** (재폭주 방지 핵심)
6. ✅ **Step 1-6. 디자인어 효과** (`server/engine/effects/mermaid.ts`)
   - `lastLevel.mermaid`와 비교해 **20점 단위**로 신규 돌파 시:
     - 우리 점수 < 상대 점수(뒤처짐): `gap = 상대점수 - 내점수`, `gain = round(gap × 0.5)` 만큼 상대로부터 흡수 (상대 점수 감소, 내 점수 증가, 이동량은 상대 보유량 한도 내)
     - 우리 점수 ≥ 상대 점수(앞섬): `bonus = round(gained × turn × 0.3)` 만큼 순수 가점 (상대 차감 없음)
   - 기존 "인어 떨림"과 달리 **뷰 직렬화 분기가 필요 없음** — 점수 갱신만으로 완결되는 서버 로직
7. ✅ **Step 1-7. 턴 진행기 & 확장 & 종료** (`server/engine/turnManager.ts`)
   - 턴 카운터, 20턴 종료 시 확장 1회, 40턴 또는 전 카드 오픈 시 종료 → 승자 판정
8. ✅ **Step 1-8. 단위 테스트** (`server/__tests__/`)
   - 규칙 케이스 15개 통과: 상표토끼 1회성 발동, 특허랑이 min 0 및 20점 단위 판정, 실용신양 연쇄 cap, 홀수 잔류, 디자인어 캐치업/리드보너스 분기 정확성 등
   - 봇 대전 시뮬레이션 500게임 완주 (크래시·무한루프 없음, 15턴 고착률 ≈ 59% 목표 범위 내)

## 🌐 Task 2. 멀티플레이 서버 (WebSocket) ✅ 완료
**목표**: N:N 실시간 대전 + 치팅 방지 뷰 분리

1. ✅ **Step 2-1. 방/로비** (`server/roomManager.ts`, `server/room.ts`)
   - 4자리 대문자 방 코드(O·I 제외) 생성, 방 생성·참가·팀 배정
   - 모든 플레이어 준비 완료 + 양팀 최소 1명 → 자동 게임 시작
2. ✅ **Step 2-2. 뷰 직렬화** (`server/serializer.ts`, `shared/protocol.ts`)
   - **미오픈 카드의 animal/num은 어떤 경우에도 전송하지 않음** — `ClientCard` 타입으로 강제
   - `GameState.board(Map)` → `ClientBoardEntry[]` 배열 직렬화 (JSON 전송용)
   - 디자인어 효과가 점수 기반으로 변경되어 팀별 뷰 분기 없음 — 단일 직렬화로 통일
3. ✅ **Step 2-3. 30초 턴 타이머** (`server/room.ts`)
   - 서버가 `turnDeadline = Date.now() + 30000` 타임스탬프 관리, 매 오픈마다 리셋
   - 시간 초과 시 `processTimeout()` 호출 → 결과 브로드캐스트 (클라 타이머는 표시 전용)
4. ✅ **Step 2-4. N:N 팀 내 로테이션** (`server/engine/turnManager.ts`)
   - `TeamState`에 `playerIndex` 추가 → 팀별 독립 인덱스 관리
   - 기존 공유 `activePlayerIndex` 방식의 2:2 버그 수정: `prevTeam.playerIndex` 증가 후 `nextTeam.playerIndex`로 activePlayerIndex 갱신
   - 검증 결과: 2:2에서 `A1→B1→A2→B2→A1→...` 순환 정확
5. ✅ **Step 2-5. 재접속/이탈 처리** (`server/room.ts`)
   - 페이지 이동 시 구 WS가 늦게 닫히는 Race Condition 수정: `handleDisconnect(playerId, ws)` — 현재 등록된 WS와 다르면 무시
   - 이탈 플레이어 차례는 30초 타임아웃 강제진행으로 자연 커버
   - 재접속: localStorage에 `cardBattle_roomId` / `cardBattle_playerId` 저장 → 페이지 재방문 시 자동 reconnect
6. ✅ **Step 2-6. 임시 클라이언트 UI** (`client/`)
   - Next.js 16 App Router, `app/page.tsx`(로비) + `app/room/[roomId]/page.tsx`(게임)
   - `hooks/useWebSocket.ts` — WebSocket 상태 관리 훅 (연결·재접속·메시지 디스패치)
   - 로비: 방 생성/참가, 팀 선택, 준비 버튼, 대기실 플레이어 목록
   - 게임: 10×10 카드 그리드, 팀별 점수판, 30초 카운트다운 타이머 바, 종료 화면

## 🎨 Task 3. 게임 화면 UI (확정 목업 기반) ✅ 완료
**목표**: 업로드한 목업 + 추가 확정사항 반영

1. ✅ **Step 3-1. 레이아웃 골격**
   - 3열 그리드: 좌(A팀 패널) / 중(카드판) / 우(B팀 패널)
   - 정글 색 팔레트 CSS 변수화 (`@theme inline`, `--color-jungle-*`, `--color-bark-*`)
2. ✅ **Step 3-2. 턴 & 타이머 바**
   - 상단 "N턴/40턴" + 프로그레스바
   - 바 맨 왼쪽 ⏳ 아이콘, 바 채움 = 남은 30초 비율 (서버 `turnDeadline` 기준 100ms 보간)
   - 10초 이하 주황, 5초 이하 빨강 + 모래시계 흔들림 (`hourglassShake @keyframes`, `prefers-reduced-motion` 대응)
3. ✅ **Step 3-3. 참가자 패널**
   - 닉네임 칩 목록, 현재 차례 플레이어 칩에 흰색 글로우 펄스 (`glowPulse @keyframes`, `prefers-reduced-motion` 대응)
4. ✅ **Step 3-4. 점수 패널**
   - 동물 이모지 2.5rem(40px→~40px), 이름·점수 함께 표시
   - 특허랑이 아래 공격력 바: "⚔ 현재 공격력 N" (`round((lastLevel.tiger+1) × turn × 1.5)`) 실시간 계산
   - 디자인어 아래 예상 획득 점수 바: 뒤처짐이면 "🔵 예상 흡수 +N" (`round(gap×0.5)`), 앞서면 "✨ 예상 보너스 +N" (`round(1×turn×0.3)`) — 별도 확정 사항으로 추가 구현됨
   - `shared/protocol.ts` `ClientTeamState`에 `lastLevel` 노출, `server/serializer.ts` 직렬화 추가
5. ✅ **Step 3-5. 카드판**
   - 마우스 휠 확대/축소 (`passive: false` wheel 이벤트) + 드래그 패닝 (pointer 이벤트, 마우스·터치 통합)
   - 드래그 이동 후 카드 클릭 차단 (이동 4px 초과 시 drag 판정)
   - 마운트 시 보드 자동 중앙 배치, `expanded` 변경(20턴) 시 재중앙화
   - 내 차례에만 카드 클릭 활성화 (`localStorage` `cardBattle_team`으로 myTeam 판별)
6. ✅ **Step 3-6. 장식 인터랙션**: 모서리 잎사귀 4개 SVG, hover 시 `leafSway @keyframes` (`prefers-reduced-motion` 대응)

## ✨ Task 4. 게임 연출 (이펙트) ✅ 완료
**목표**: 규칙 이벤트마다 "보이는 재미" 부여

1. ✅ **Step 4-1. 카드 플립 공통 애니메이션**
   - CSS `rotateY` 3D 플립 (뒷면 → 앞면), 지속시간 ~250ms (`cardFlipOut` 125ms + `cardFlipIn` 125ms)
   - `useAnimationQueue` 훅의 `suppressedKeys` Set으로 플립 타이밍 제어 — `isSuppressed: true → false` 전환 시 `CardCell` 내부에서 자동 flip 트리거
   - `prefers-reduced-motion` 대응

2. ✅ **Step 4-2. 숫자별 리액션 연출 (1-8절 스펙 구현)**
   - `num === 1`: 회색 점 파티클 4개 (opacity fade + floatUp)
   - `num in [2,3]`: `.card-wink` (`@keyframes wink`, 동물 이모지 scaleY 0.15 축소 후 복귀)
   - `num in [4,5]`: 윙크 + 👍 배지 bounce pop
   - `num === 6`: 윙크 + 👍 + `.card-gold-flash` (금빛 box-shadow 펄스 + scale 1.08)
   - `transform`, `opacity` 위주 — 레이아웃 리플로우 없음

3. ✅ **Step 4-3. 짝수 매칭 성공 연출**
   - `collect` 이벤트 시 해당 동물 점수 행에 `.score-pop` (`@keyframes scorePop`, 400ms 팝)
   - `scoreFlash` Map으로 team:animal별 독립 트리거

4. ✅ **Step 4-4. 실용신양 연쇄 오픈 & 레벨별 화면 흔들림 (1-9절 스펙 구현)**
   - `sheepChain` 이후 `open` 이벤트들 55ms 스태거 플립 (최대 15장 시각화)
   - `n=1~4`: `.shake-small` / `n=5~7`: `.shake-medium` + 나뭇잎 4개 낙하 / `n≥8`: `.shake-strong` + 나뭇잎 8개 + 미오픈 카드 전체 `.card-jolt`
   - `prefers-reduced-motion`: 흔들림/낙하 생략, 리액션 유지

5. ✅ **Step 4-5. 상표토끼 보너스 — 토끼 집결 연출**
   - `rabbitBonus` 이벤트 시 🐰 8마리가 8방향에서 아이콘으로 수렴 (`@keyframes rabbitRush`, 개별 delay)
   - 착지 후 scale 0으로 흡수 + "+N" 플로팅 텍스트

6. ✅ **Step 4-6. 디자인어 캐치업/리드보너스 연출**
   - `mermaidCatchup`: 뒤처진 팀 패널에 파란 파동 (`@keyframes mermaidWave`) + "+N"/"-N" 플로팅 텍스트
   - `mermaidBonus`: 앞선 팀 패널에 파동 + "+N" 플로팅 텍스트

7. ✅ **Step 4-7. 특허랑이 공격**
   - `tigerAttack` 시 상대 팀 합계 패널에 빗금 오버레이 (`@keyframes tigerSlash`) + "-N" 빨간 플로팅 텍스트

8. ✅ **Step 4-8. 보드 확장**
   - `expand` 이벤트 시 카드 그리드 scale 1 → 0.93 → 1 숨쉬기 (`@keyframes boardBreathe`, 500ms)

9. ✅ **Step 4-9. 종료**
   - 승리 팀 색상 confetti 45개 낙하 (`@keyframes confettiFall`) + 텍스트 `bounceIn` + 패배 팀 opacity 감소

**아키텍처**: `useAnimationQueue` 훅이 `lastEvents[]`를 직렬 큐로 처리. 서버 코드 수정 없는 클라이언트 전용 구현.

### Task 4 보완 — 실플레이 QA 후 2차 정련 ✅ 완료

M2/M3 완료 직후 실제 브라우저 2개로 플레이해 보며 나온 버그 수정 및 도파민 강화 요청을 반영한 기록.

1. **클릭 먹통 버그 수정** — `BoardPanel`의 드래그 패닝이 `pointerdown` 즉시 `setPointerCapture`를 걸어, 단순 클릭까지 컨테이너로 리타깃되어 카드 `onClick`이 아예 발화하지 않던 문제. 실제 이동 임계값(3px)을 넘는 순간에만 캡처하도록 수정.
2. **세션 충돌 안내** — 같은 브라우저의 새 창은 `localStorage`(재접속용 `roomId`/`playerId`)를 공유해 두 번째 창이 첫 플레이어 세션을 가로채는 현상 확인. 로컬 테스트 시 반드시 별도 브라우저(또는 시크릿 창)를 사용하도록 안내(코드 버그 아님).
3. **페어 매칭 카드 표시 방식 변경** — 매칭 성공 시 카드가 즉시 사라지던 것을 0.5초간 테두리가 회전하는 그라데이션으로 반짝인 뒤(8색 랜덤), A팀은 채도 낮은 녹색, B팀은 채도 낮은 남색으로 **비활성화된 채 계속 보이게** 변경(어떤 카드였는지 계속 식별 가능).
4. **미매칭 오픈 카드 팀 색 테두리** (`Card.openedBy` 필드 추가) — 오픈했지만 짝을 못 찾은 카드는 오픈한 팀 색 테두리(A=초록/B=남색)를 유지하다가, **그 팀이 다음으로 카드를 여는 시점**(상대 턴 내내 + 자기 턴이 와도 실제 오픈 전까지)에 정확히 얇은 회색 테두리로 되돌아가도록 서버에서 처리 — 직전 턴에 우리 팀이 무엇을 뽑았는지 판단 근거로 활용 가능.
5. **카드 뒷면 턴 표시 이동** — 원래 있던 팀 패널 배경색 변경은 되돌리고, 실제로 클릭해야 하는 보드 위 "?" 카드에 적용: 내 차례엔 흰 바탕에 반짝이는 사선 광택 애니메이션(`card-shiny`)으로 클릭을 유도, 상대 차례엔 옅은 회색으로 비활성 표시. 모든 카드(오픈/미오픈/수집됨)에 턴과 무관하게 동일한 약한 그림자(`card-shadow`) 적용.
6. **보드 확장 연출 강화** — 단순 숨쉬기 애니메이션에 지진(`quake-expand`, 스케일 펄스 포함 강한 흔들림) + 노란 충격파 링 3개 + 화면 섬광 플래시 + 떨어지는 먼지/잔해 파티클(💨🪨💥)을 추가해 "지각변동" 느낌으로 강화. 새로 증축된 외곽 링 카드는 뒷면 배경을 더 진하게 구분.
7. **해설판(`CommentaryBoard`) 신설** — 팀별로 나뉘어 있던 로그를 보드 하단에 통합, `[A팀]/[B팀]` 주체 태그 + "이모지 이름 +점수! (변동전 → 변동후)" 형식으로 통일(최종 `gameState` 점수에서 역산해 정확한 전/후 값 계산). 로그가 최대 줄 수에 도달해도 항상 최신 줄까지 자동 스크롤.
8. **점수판 레이아웃 개편** — 동물 이름 텍스트를 빼고 아이콘+점수만 표 형태(1×4 세로 배치)로 확대. 실용신양·상표토끼에도 특허랑이·디자인어처럼 상시 배지 추가: "추가 카드 오픈 +N장"(실용신양 연쇄로 지금까지 열린 누적 카드 수 — 라이브 카운터로 정상 동작 확인용), "다음 보너스 +N점"(상표토끼).
9. **상표토끼 보너스 연출 고도화** — 막연한 집결 애니메이션 대신, 보드에서 실제로 그 팀의 뒤집힌/수집된 토끼 카드 위치를 `getBoundingClientRect`로 측정해 그 자리에서 점수판의 상표토끼 칸으로 진짜로 날아가는 연출(`RabbitFlightLayer`)로 교체.
10. **특허랑이 공격 연출 고도화** — 공격팀 패널이 바깥으로 살짝 빠졌다가 게임판 쪽으로 강하게 슬램(`panel-recoil-a/b`), 피격팀 패널 전체가 들썩이고(`panel-hit-shake`), 실용신양·상표토끼 표가 각각 한 번 더 들썩이며 정확한 위치에서 빨간 "-N"이 페이드아웃 플로팅(`table-hit`/`table-hit-float`) — 기존엔 위치가 부정확한 범용 플로팅 텍스트가 중복 표시되던 것을 제거.
11. **디자인어 팝업 신설** — 발동 팀 쪽(좌/우, 게임판 하단·해설판 바로 위)에서 큰 인어 이모지가 튀어나와 부르르 떠는 동안 음표(🎵🎶)가 팝콘처럼 튀는 2초 연출(`MermaidPopup`) 추가.
12. **실용신양 효과 수식 수정** — 새로 넘은 구간 수(`gained`, 보통 1)만큼만 추가 오픈하던 것을, 기획 의도("정교함보다 물량")에 맞게 **도달한 누적 레벨(n)** 만큼 추가 오픈하도록 수정 — 점수가 쌓일수록 한 번의 발동에서 열리는 카드 수도 함께 늘어난다(연쇄 중 새 구간을 또 넘으면 그 증가분도 그대로 합산, 안전 상한 350은 유지).

### Task 4 보완 — 3차 정련 (실용신양 상시효과 재설계 등) ✅ 완료

2차 정련의 실용신양 수식 수정도 여전히 "새 임계값을 넘을 때"라는 조건부 발동이었는데, 실플레이 결과 원래 기획 의도와 다르다는 게 확인되어 근본적으로 다시 설계함.

1. **실용신양 = 상시효과로 전면 재설계** (`server/engine/effects/sheep.ts`, `server/engine/openCard.ts`) — "새 임계값을 처음 넘을 때 1회 발동"이라는 조건부 게이트(`lastLevel` 기반)를 완전히 제거. 이제 **매 턴(어떤 동물 카드를 열었든, 매치가 있었든 없었든)** 그 팀의 **현재** 실용신양 점수(`floor(score/10)`)만큼 항상 카드를 추가로 연다. `applySheepEffect` 호출 위치를 `_openCard`의 매치 분기에서 `openCard()` 최상단(턴당 정확히 1회)으로 이동. 점수가 오르면 다음 턴 추가 오픈 수도 늘고, 특허랑이 공격으로 점수가 깎이면 다음 턴 즉시 줄어든다.
2. **상표토끼 재발동 버그 수정** (`server/engine/effects/tiger.ts`) — 특허랑이 공격으로 상대의 실용신양/상표토끼 점수를 깎을 때 `lastLevel`이 하락 없이 그대로 남아 있어, 한 번 넘었던 임계값을 점수가 깎였다가 다시 넘어도 상표토끼 보너스가 재발동하지 않는 버그가 있었음. 점수를 깎을 때 `lastLevel`도 `Math.min(기존값, floor(현재점수/threshold))`로 함께 낮추도록 수정.
3. **치명적 무한루프 버그 발견 및 수정** (`server/engine/turnManager.ts`) — 위 상시효과 재설계로 한 팀의 단일 턴 안에서 보드 전체가 소진될 수 있게 되면서, "B팀 턴이 끝난 후에만 전체 오픈 여부를 검사"하던 기존 종료 판정 로직이 A팀 턴 중 보드가 다 소진돼도 게임을 끝내지 못해 다음 팀이 열 카드가 없는 채로 멈춰버리는(무한루프) 버그를 발견 — 어느 팀 턴이 끝났든 매번 종료 조건을 검사하도록 수정.
4. **점수판 배지 실시간화** — 2차 정련 때 "누적 카운터"였던 "추가 카드 오픈 +N장"을 제거하고, `ScorePanel`이 이미 갖고 있는 현재 점수에서 `Math.floor(scores.sheep / THRESHOLDS.sheep)`로 매 렌더마다 직접 계산하도록 변경 — 별도 state 없이 항상 정확한 실시간 값.
5. **인어 팝업 크기 보정** — 이전에 음표만 키우고 인어 본체는 그대로 두었던 실수 수정. 인어 이모지에 별도 `font-size`(6.5rem)를 지정하고, 음표들의 확산 반경도 비례해 넓혔다.
6. **게임 종료 화면 — 승패 요인 동물 안내** (`GameEndScreen.tsx`) — "X팀 승리!"만 보여주던 것에, 승리팀이 패배팀보다 가장 크게 앞선 동물을 정규화 격차비율(`(승-패)/(승+패)`)로 판정해 그 동물에 맞는 짧은 플레이버 문구를 추가(예: 특허랑이 압승 → "가장 강력한 독점권을 행사하셨군요!"). 무승부일 땐 표시하지 않음.
7. **회귀 테스트 추가** (`server/__tests__/effects.test.ts`) — 실용신양이 `lastLevel`과 무관하게 항상 현재 점수 기준으로 발동하는지, 특허랑이 공격 후 상표토끼 `lastLevel`이 clamp되어 재발동 가능해지는지 검증하는 테스트 2종 추가 (총 23개 테스트 통과).

### Task 4 보완 — 4차 정련 (사운드 이펙트 + 실용신양 콤보 연출 + IP 현황 바 + 애니메이션 재생 버그 수정) ✅ 완료

1. **동물별 사운드 이펙트 추가** — `client/app/api/sounds/route.ts`가 `public/sounds/` 폴더를 스캔해 `sheep_`/`mermaid_`/`tiger_`/`rabbit_` 접두사별 파일 목록을 반환하고, `client/lib/sounds.ts`가 이를 캐싱해 무작위 재생을 담당. 실용신양은 연쇄 추가 오픈 카드마다(기본 오픈은 무음), 디자인어는 팝업 시작 시점, 특허랑이는 상대 패널 가격 임팩트 순간, 상표토끼는 보너스 점수 자릿수만큼(1자리=1개, 2자리=2개…) 착지 시점에 순차 재생. 게임 종료 시 `result.wav` 1회 재생.
2. **실용신양 연쇄 오픈 페이싱 재설계** (`useAnimationQueue.ts`) — 카드 간 간격을 55ms → **500ms**로 늘려 한 장씩 순차적으로 열리는 "우당탕탕" 느낌을 구현. 연쇄 도중 짝이 맞아 발생하는 `collect`/특허랑이/디자인어 이벤트가 아직 다 열리지 않은 시점의 낡은 타임라인 기준으로 재생되던 버그도 함께 수정 — 이제 매 카드 리빌마다 타임라인 커서를 갱신해, 정산 연출은 항상 그 시점까지 열린 카드 다음에 재생된다.
3. **실용신양 콤보 텍스트** (`SheepComboLayer.tsx`) — 추가 오픈되는 카드마다 그 카드 중앙 위치에서 "N combo!" 텍스트가 위로 플로팅하며 페이드아웃. 노랑~주황 그라데이션 + 두꺼운 폰트 + 60% 불투명 검정 1px 외곽선 + 그림자, 폰트 크기는 기본 1.3rem에서 콤보당 10%씩 증가.
4. **콤보별 화면 진동** — 기존엔 연쇄 시작 시 1회만 흔들리던 것을, **콤보(추가 오픈 카드)마다** 흔들리도록 변경. 1콤보는 약하게 시작해 콤보 번호가 오를수록 진동 강도가 연속적으로(3단계 계단이 아니라 `--shake-scale` CSS 변수 기반 선형 스케일로) 점점 강해짐.
5. **IP 오픈 현황 바** (`IpStatusBar.tsx`) — 해설판 헤더에 동물별(양·토끼·인어·호랑이 순) 그래픽 프로그레스바 추가. 바 중앙에 "오픈카드/전체카드" 표시, 동물별 색상 구분. `gameState`에서 매 렌더마다 직접 계산하는 파생값이라 카드 오픈·보드 확장(전체 수량이 25 → 49로 변경) 시 별도 처리 없이 항상 최신 상태 반영.
6. **애니메이션 재생 중복 버그 수정** (`useAnimationQueue.ts`) — `useEffect` 의존성 배열에 `gameState`가 포함되어 있어, 재접속 시 오는 `gameSnapshot`처럼 **새 이벤트 없이 gameState만 갱신되는 경우에도** 지난 턴의 전체 애니메이션(특히 페어 매칭 글로우)이 처음부터 재생되는 버그를 발견 — 브라우저 탭 2개에서 같은 카드가 한쪽은 정상적으로 팀 색으로 비활성화되어 보이고 다른 한쪽은 아직 안 끝난 것처럼 글로우 테두리로 보이는 불일치의 원인이었음. `gameState`를 ref로 옮겨 effect는 `lastEvents`가 바뀔 때만 실행되도록 수정.

## 🧪 Task 5. 통합 테스트 & 밸런스 재검증 ⬜ 미구현
1. 봇 vs 봇 자동 대전 모드(서버 내장)로 e2e 흐름 검증
2. 실제 구현 위에서 시뮬레이션 재실행 → 아래 목표 지표와 일치하는지 확인
   - 특허랑이 억제력/상표토끼 폭증력 비율 ≈ **0.81**
   - 디자인어 15턴 리더 고착률 ≈ **59%**, 역전 성공률 ≈ **41%**, 최종 격차 평균 ≈ **26**
3. 30초 타이머·강제진행·재접속 시나리오 수동 테스트
4. 브라우저/모바일 반응형 QA

## 🚀 Task 6. 배포 & 운영 준비 ⬜ 미구현
1. 서버 호스팅(예: Fly.io/Render 등 WebSocket 지원) + 클라이언트 정적 호스팅
2. 방 코드 공유 방식(URL 초대 링크)
3. 간단한 관전 모드(선택), 게임 로그 저장(밸런스 데이터 수집용)

---

# PART 4. 권장 진행 순서 & 마일스톤

| 마일스톤 | 포함 Task | 완료 기준 | 상태 |
|---|---|---|---|
| **M1. 규칙이 돌아간다** | Task 0, 1 | UI 없이 봇 대전이 40턴 완주, 테스트 통과 | ✅ 완료 |
| **M2. 둘이서 논다** | Task 2 | 브라우저 2개로 N:N 실시간 대전 가능 (임시 UI) | ✅ 완료 |
| **M3. 보기 좋다** | Task 3 | 확정 목업대로 화면 완성, 타이머·글로우·공격력 바 동작 | ✅ 완료 |
| **M4. 재밌다** | Task 4 | 전 이벤트 연출 탑재 + 실플레이 QA 기반 2차 정련(버그 수정·연출 고도화) | ✅ 완료 |
| **M5. 내보낸다** | Task 5, 6 | QA 통과, 초대 링크로 친구와 플레이 가능 | ⬜ 미구현 |

> ⚠️ 우선순위 팁: Task 1(엔진)을 UI와 분리해 먼저 완성하는 것이 가장 중요. 규칙이 복잡해서(연쇄 오픈, lastLevel, 턴 종료 훅, 동물별 상이한 threshold) UI에 섞어 짜면 디버깅이 매우 어려워짐. M1에서 자동 대전 수백~1000판을 돌려 안정성과 밸런스 지표를 함께 확보한 뒤 화면 작업으로 넘어갈 것.

---

# PART 5. 개발 환경 & 실행 방법

## 5-1. 디렉토리 구조

```
card_battle/
├── shared/          # 타입·상수·WebSocket 프로토콜 (양쪽에서 공유)
│   ├── types.ts     # GameState, Card, TeamState (playerIndex 포함) 등
│   ├── constants.ts # THRESHOLDS, 계수, 턴 상한 등
│   └── protocol.ts  # ClientMessage, ServerMessage, ClientGameState 등
├── server/          # Node.js 게임 서버
│   ├── index.ts     # WebSocket 서버 진입점 (포트 8080)
│   ├── room.ts      # Room 클래스 (플레이어 관리·타이머·브로드캐스트)
│   ├── roomManager.ts
│   ├── serializer.ts  # 뷰 직렬화 (치팅 방지)
│   └── engine/      # 게임 엔진 (Task 1, UI와 완전 분리)
│       ├── gameEngine.ts   # processPlayerAction, processTimeout, initGame
│       ├── board.ts
│       ├── openCard.ts
│       ├── turnManager.ts
│       └── effects/  # sheep / rabbit / mermaid / tiger
└── client/          # Next.js 16 앱 (임시 UI, Task 3에서 교체 예정)
    ├── app/
    │   ├── page.tsx              # 로비 (방 생성/참가/대기)
    │   └── room/[roomId]/page.tsx  # 게임 화면
    └── hooks/
        └── useWebSocket.ts      # WebSocket 상태 관리 훅
```

## 5-2. 개발 서버 실행

**터미널을 두 개 열어 각각 실행한다.**

```bash
# 터미널 1 — WebSocket 게임 서버 (포트 8080)
cd server
npx ts-node index.ts

# 터미널 2 — Next.js 클라이언트 (포트 3000)
cd client
npm run dev
```

브라우저에서 `http://localhost:3000` 접속.

## 5-3. 중단 방법

두 터미널 모두 **`Ctrl+C`** 로 종료.

> 포트가 이미 사용 중이라는 오류가 나면 PowerShell에서:
> ```powershell
> # 8080 포트 점유 프로세스 종료
> Stop-Process -Id (Get-NetTCPConnection -LocalPort 8080).OwningProcess -Force
> ```

## 5-4. 테스트 실행

```bash
cd server

npm test              # 전체 단위 테스트 (20개)
npm run test:sim      # 봇 500게임 시뮬레이션 (시간 초과 방지 옵션 포함)
```

## 5-5. 주요 포트 & 환경 변수

| 항목 | 기본값 | 변경 방법 |
|---|---|---|
| WebSocket 서버 포트 | `8080` | `PORT=9000 npx ts-node index.ts` |
| Next.js 포트 | `3000` | `npm run dev -- -p 3001` |
| 클라이언트가 바라보는 WS 주소 | `ws://localhost:8080` | `NEXT_PUBLIC_WS_URL` 환경 변수 |

## 5-6. N:N 대전 테스트 방법

1. 서버와 클라이언트 실행
2. 브라우저 탭(또는 창) **N개** 열기
3. 첫 번째 탭: **방 만들기** → 방 코드 확인 (예: `ABCD`)
4. 나머지 탭: **방 참가하기** → 방 코드 입력, 각자 팀 선택
5. 모든 참가자 **준비** 버튼 클릭 → 게임 자동 시작
6. 차례인 플레이어의 탭에서 카드 클릭 → 전체 탭에 즉시 반영
